# k1maxeddyng
Attempt number2 at EddyNG support on K1Max stock firmware if it works hopefully it will work with the CFS firmware as well
backs up and overwites
sudo ./eddyngfiles.sh update
restores original files
sudo ./eddyngfile.sh restore ~/klipper_backups/<insert file created by update>.tar.gz

